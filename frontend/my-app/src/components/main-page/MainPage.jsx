import React from 'react';
import ResponsiveAppBar from '../navbar/Navbar';
import { motion } from 'framer-motion';
const MainPage = (props) => {
	return (
		<div>
			<motion.div className="main-page-container"></motion.div>
		</div>
	);
};

export default MainPage;
